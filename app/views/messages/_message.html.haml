.message
  .message-info
    .message-info__user
      = message.user.name
    .message-info__date
      = message.created_at.strftime("%Y/%m/%d %H:%M")
  - if message.body.present?
    %p.message__text
      = message.body
  = image_tag message.image_url if message.image.present?
